# Core dependencies for the recommender system (CLI demo + evaluation).
# Tested with Python 3.11.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10

# Optional — only needed for the web UI (app_streamlit.py):
# streamlit>=1.30
